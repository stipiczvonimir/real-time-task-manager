{
  "name": "real-time-task-manager",
  "private": true,
  "scripts": {
    "server": "dotnet run --project server/TaskManagement.Server",
    "web": "cd web-client && npm run dev",
     "start": "echo Backend: http://localhost:5138 && echo Web UI: http://localhost:5173 && concurrently \"npm run server\" \"npm run web\""
 
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}